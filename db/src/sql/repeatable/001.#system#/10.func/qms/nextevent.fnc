CREATE OR REPLACE FUNCTION qms.nextevent(
  a_queue_id IN VARCHAR
)
  RETURNS BIGINT AS
$$

SELECT id
FROM qms.queue_event
WHERE queue_id = a_queue_id
ORDER BY
  queue_id,
  id ASC
LIMIT 1;

$$
LANGUAGE SQL STABLE
RETURNS NULL ON NULL INPUT
SECURITY DEFINER;
/